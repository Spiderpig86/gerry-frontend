import { IDemographics, IVoteData } from '../../models';

export class ModelMapper {

    public toIDemographic(demographic: any): IDemographics {
        return {
            
        }
    }

    public toIVote(vote: any): IVoteData {
        return {
            
        }
    }

}